/**
 * Parameters that can adjust how the generation is done.
 * They are influencing specific settings and can define new limits.
 * Check the different values for details.
 */
export class DnaGenerationParameters {
  public allowColoredEyes = false;
}
